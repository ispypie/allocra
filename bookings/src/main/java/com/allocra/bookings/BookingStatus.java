package com.allocra.bookings;

/**
 * Booking lifecycle states for the initial release (PRD-BKG-002). Holds are
 * deferred.
 */
public enum BookingStatus {
	CONFIRMED, CANCELLED, COMPLETED, NO_SHOW
}
